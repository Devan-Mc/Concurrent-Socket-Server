# Concurrent-Socket-Server
These are 2 programs that create a multiple clients that interact with the concurrent socket server which handles multiple client connections simultaneously using sockets and multiple threads.
